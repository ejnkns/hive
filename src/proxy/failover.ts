// failover.ts deleted — the active pipeline uses executeProxyRequest
// with RoutingMemory for node selection and failover. The duplicated
// sanitizePayloadForProvider is now solely in src/engine.ts.
